<import src="../../templates/hero.wxml" />
<import src="../../templates/card.wxml" />

<template is="hero" data="{{heroData}}" />

<view class="wrap tabpage">
  <view class="filterbar">
    <picker mode="selector" range="{{filters}}" value="{{filterIndex}}" bindchange="onFilter">
      <view class="filterpick">{{filters[filterIndex]}}</view>
    </picker>
  </view>

  <block wx:if="{{gapItems.length}}">
    <view class="card-group">
      <view class="sec-title">
        <image class="seal-icon" src="/assets/seal-dot.svg" mode="aspectFit" />
        <text class="n alert">!</text>你可能漏掉的
      </view>
      <view class="card-inset">
        <block wx:for="{{gapItems}}" wx:key="id">
          <template is="card" data="{{c: item}}" />
        </block>
      </view>
    </view>
  </block>

  <view class="card-group">
    <view class="sec-title"><text class="n">{{items.length}}</text>全部机会</view>
    <block wx:if="{{items.length}}">
      <view class="card-inset">
        <block wx:for="{{items}}" wx:key="id">
          <template is="card" data="{{c: item}}" />
        </block>
      </view>
    </block>
    <view wx:else class="empty">
      <image class="empty-img" src="/assets/empty-signal.svg" mode="aspectFit" />
      <view class="empty-title">{{online ? '暂无匹配活动' : '未连接后端'}}</view>
      <view class="small">{{online ? '粘贴班委群消息到「导入」，或等待官网采集' : '启动 poke-server 后下拉刷新'}}</view>
    </view>
  </view>
</view>
